# yolo8 wave and object

This branch contains ONLY the catkin workspace `src/` for:
- YOLOv8 person detection
- Wave (gesture) recognition
- Object-related perception / bridge nodes

No build/, devel/, logs/ are included.
